{
  "name": "examples-cloudflare-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "examples/cloudflare-app/src",
  "targets": {
    "build": {
      "executor": "@storm-stack/nx:cloudflare-app-build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/examples/cloudflare-app",
        "skipCache": true,
        "skipInstalls": true
      }
    }
  }
}
