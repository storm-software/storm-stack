{
  "id": "#",
  "title": "Storm Stack Prepare Executor",
  "description": "A type definition for the Storm Stack - Prepare executor schema",
  "required": [],
  "properties": {
    "outputPath": {
      "title": "Output Path",
      "type": "string",
      "format": "path",
      "description": "The output path for the build",
      "id": "#outputPath",
      "default": "dist/{projectRoot}"
    },
    "tsconfig": {
      "title": "TypeScript Configuration File",
      "type": "string",
      "format": "path",
      "description": "The path to the tsconfig file",
      "id": "#tsconfig",
      "default": "{projectRoot}/tsconfig.json"
    },
    "mode": {
      "title": "Mode",
      "type": "string",
      "description": "The build mode",
      "enum": [
        "development",
        "staging",
        "production"
      ],
      "id": "#mode"
    },
    "skipInstalls": {
      "title": "Skip Installs",
      "type": "boolean",
      "description": "Skip installing dependencies during prepare stage",
      "id": "#skipInstalls"
    },
    "skipCache": {
      "title": "Skip Cache",
      "type": "boolean",
      "description": "Skip the cache when building",
      "id": "#skipCache"
    },
    "logLevel": {
      "title": "Log Level",
      "type": "string",
      "description": "The log level to use for the build process",
      "enum": [
        "fatal",
        "error",
        "warn",
        "success",
        "info",
        "debug",
        "trace",
        "silent"
      ],
      "id": "#logLevel"
    },
    "clean": {
      "title": "Clean",
      "type": "boolean",
      "description": "Automatically clean the project's output artifacts (if required)",
      "id": "#clean"
    }
  },
  "type": "object",
  "default": {
    "outputPath": "dist/{projectRoot}",
    "tsconfig": "{projectRoot}/tsconfig.json"
  }
}